import { Card } from "@molecules/Card";

export function Result() {

    return (
        <>
            {/* {data.map(vehicle => (
                <Card key={vehicle.id} type="result" data={vehicle} />
            ))} */}
        </>
    );
}